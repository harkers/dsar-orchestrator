"""Operator-console helpers that talk to the local mlx-broker / case data.

Stage summaries, dedupe filtering, closure-letter drafting, unextractable
triage and leak-review live here; the console imports them from ``..local_broker``.
"""
